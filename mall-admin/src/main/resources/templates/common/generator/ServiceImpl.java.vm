package ${package}.${module}.service.impl;

import ${package}.${module}.entity.${Module}${className};
import ${package}.${module}.mapper.${Module}${className}Mapper;
import ${package}.${module}.service.I${Module}${className}Service;
import com.baomidou.mybatisplus.extension.service.impl.ServiceImpl;
import org.springframework.stereotype.Service;

/**
 * <p>
 * 服务实现类
 * </p>
 *
 * @author zscat
 * @since 2019-04-14
 */
@Service
    public class ${Module}${className}ServiceImpl extends ServiceImpl<${Module}${className}Mapper, ${Module}${className}> implements I${Module}${className}Service {
}
