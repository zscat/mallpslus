package ${package}.${module}.service;

import ${package}.${module}.entity.${Module}${className};
import com.baomidou.mybatisplus.extension.service.IService;

/**
 * ${comments}
 *
 * @author ${author}
 * @email ${email}
 * @date ${datetime}
 */

public interface I${Module}${className}Service extends IService<${Module}${className}> {

}
